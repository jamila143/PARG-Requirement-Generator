# Python
backend/venv/
__pycache__/
*.pyc
*.pyo

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/.env

# Trained model weights -- GitHub hard-blocks any file over 100MB.
# ner_model_state_dict.pt and classifier_model_state_dict.pt are ~430MB
# each and must NEVER be committed. See README for how to share them
# separately (Google Drive / Git LFS / Kaggle output link).
backend/models/*.pt

# Local runtime database (created automatically, don't commit personal data)
backend/parg_history.db

# OS files
.DS_Store
Thumbs.db
